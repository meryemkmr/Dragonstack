const SECONDS = 1000;
const MINUTES = SECONDS * 60
const HOURS = MINUTES * 60
const DAY = HOURS * 24

const RESRESH_RATE = 5 //units


module.exports = {
    SECONDS,
    MINUTES,
    HOURS,
    DAY,
    RESRESH_RATE
}

